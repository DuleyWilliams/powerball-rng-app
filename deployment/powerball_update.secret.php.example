<?php

declare(strict_types=1);

// Copy this file to powerball_update.secret.php (same directory) on the
// server and replace the value below with a long random token. That file
// is gitignored — never commit the real value.
//
// Generate one with any of:
//   php -r "echo bin2hex(random_bytes(32)), PHP_EOL;"
//   openssl rand -hex 32
//
// powerball_update.php also accepts the token via a POWERBALL_CRON_TOKEN
// environment variable instead of this file, if your hosting setup makes
// that easier to manage — the env var takes priority when both are set.

return 'REPLACE_THIS_WITH_A_LONG_RANDOM_TOKEN_BEFORE_DEPLOYING';
